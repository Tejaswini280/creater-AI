# Minimal nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:simple"]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
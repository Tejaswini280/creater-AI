# Railway Simple - Minimal configuration to avoid Docker issues
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:simple"]

[start]
cmd = "node dist/index.js || npm start"
